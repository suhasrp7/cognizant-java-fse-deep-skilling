import { CourseTitlePipe } from './course-title-pipe';

describe('CourseTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
